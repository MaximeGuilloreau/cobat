{% extends 'base.html.twig' %}

{% block body %}
    <section class="content-header">
        {#<ol class="breadcrumb">#}
            {#<li><a href="#"><i class="fa fa-dashboard"></i> Level</a></li>#}
            {#<li class="active">Here</li>#}
        {#</ol>#}

        <h1>
            Liste des Chantier
        </h1>
    </section>

    <section class="content">
        <table class="table" >
            <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for site in sites %}
                <tr>
                    <td><a href="{{ path('site_show', { 'id': site.id }) }}">{{ site.id }}</a></td>
                    <td>{{ site.name }}</td>
                    <td>
                        <a class="btn" href="{{ path('site_show', { 'id': site.id }) }}">Ouvrir</a>
                        <a class="btn" href="{{ path('site_edit', { 'id': site.id }) }}">Modifier</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <ul>
            <li>
                <a class="btn" href="{{ path('site_new') }}">Créer un nouveau chantier</a>
            </li>
        </ul>
    </section>
{% endblock %}
